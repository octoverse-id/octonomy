[project]
name = "octonomy"
version = "3.1.1"
description = "Multi-tenant tag management and taxonomy service."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = [
  "django>=5.2.8,<5.3",
  "djangorestframework>=3.15,<3.18",
  "drf-spectacular>=0.27,<0.31",
  "django-unfold>=0.101,<0.103",
  "psycopg[binary]>=3.2,<3.4",
  "dj-database-url>=2.2,<4.0",
  "python-dotenv>=1.0,<2.0",
  "gunicorn>=23.0,<24.0",
  "whitenoise>=6.12,<7.0",
]

[project.optional-dependencies]
dev = [
  "factory-boy>=3.3,<3.4",
  "pytest>=8.3,<10.0",
  "pytest-django>=4.9,<5.0",
  "pytest-cov>=5.0,<8.0",
  "pip-audit>=2.7,<3.0",
  "ruff>=0.8,<0.17",
]

[tool.pytest.ini_options]
# config.settings_pytest is config.settings plus one documented override (the
# staticfiles storage backend). See that module for why.
DJANGO_SETTINGS_MODULE = "config.settings_pytest"
python_files = ["test_*.py", "*_test.py"]
testpaths = ["tests"]
addopts = "-ra --cov=octonomy --cov-report=term-missing"
filterwarnings = [
  # WhiteNoise warns once per middleware instantiation when STATIC_ROOT is absent, and
  # under pytest it usually is: /staticfiles/ is gitignored and no test needs it. The
  # tests that do care build their own collected STATIC_ROOT
  # (tests/admin/test_static_serving.py), so nothing is hidden here except ~50 repeats
  # of a message about a directory the suite deliberately does without.
  "ignore:No directory at:UserWarning",
]

[tool.coverage.run]
source = ["octonomy"]
omit = ["*/migrations/*", "*/tests/*"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B904"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
